{
  "name": "json-server",
  "version": "1.0.0",
  "description": "A simple JSON server for serving and managing JSON data from assets folder",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}